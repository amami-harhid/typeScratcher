export type RGBColorArray = number[];

export  type RGBObject = {
    r: number,
    g: number,
    b: number,
    a?: number,
}
export  type HSVObject = {
    h: number, 
    s: number,
    v: number,
}