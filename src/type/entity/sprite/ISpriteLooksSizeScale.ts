export interface ISpriteLooksSizeScale {
    /**
     * スケール幅（％）
     */
    get w(): number;

    /**
     * スケール高さ（％）
     */
    get h(): number;

        /**
     * スケール幅（％）
     */
    set w(w:number);

    /**
     * スケール高さ（％）
     */
    set h(h:number);

}