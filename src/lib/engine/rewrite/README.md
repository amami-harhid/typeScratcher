# 概要

ASTを使いfor文にyieldを自動挿入したらどうかと思っての作りかけ。
しかし対応可能な範囲（種類）が限定的になるので、途中で仕上げを放棄中。

# 対応可能な範囲（種類）が限定的とは？

イベント処理の function 内は対応可能だが、
function内で ローカルに別のfunctionを書かれたりする( 内部でfor文をもつfunction )
ときに、別functionを呼び出すほうで、yield * で呼び出すことになり、
収拾がつかないと判断。

現状どおり、Elint でfor文警告を出し、yieldを明示的に書いてもらうことにする。

